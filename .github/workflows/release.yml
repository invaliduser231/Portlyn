name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build static frontend
        run: |
          cd frontend
          npm ci --no-audit --no-fund
          PORTLYN_STATIC_EXPORT=1 npm run build
          rm -rf ../cmd/server/frontend_dist
          mv out ../cmd/server/frontend_dist

      - name: Build server binaries
        env:
          CGO_ENABLED: "0"
        run: |
          mkdir -p dist
          for target in linux/amd64 linux/arm64 darwin/amd64 darwin/arm64 windows/amd64 windows/arm64; do
            os="${target%/*}"
            arch="${target#*/}"
            ext=""
            if [ "$os" = "windows" ]; then ext=".exe"; fi
            GOOS="$os" GOARCH="$arch" go build -trimpath -ldflags="-s -w" -o "dist/portlyn-$os-$arch$ext" ./cmd/server
          done

      - name: Verify embedded frontend
        run: |
          if ! ls cmd/server/frontend_dist/index.html >/dev/null 2>&1; then
            echo "::error::cmd/server/frontend_dist/index.html missing - server binaries would ship empty UI"
            exit 1
          fi

      - name: Build node agent binaries
        env:
          CGO_ENABLED: "0"
        run: |
          for target in linux/amd64 linux/arm64 darwin/amd64 darwin/arm64 windows/amd64 windows/arm64; do
            os="${target%/*}"
            arch="${target#*/}"
            ext=""
            if [ "$os" = "windows" ]; then ext=".exe"; fi
            GOOS="$os" GOARCH="$arch" go build -trimpath -ldflags="-s -w" -o "dist/portlyn-nodeagent-$os-$arch$ext" ./cmd/nodeagent
          done

      - name: Create checksums
        run: |
          cd dist
          sha256sum -- * > checksums.txt

      - name: Install cosign
        uses: sigstore/cosign-installer@v3.6.0

      - name: Sign checksums (keyless)
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: cosign sign-blob --yes dist/checksums.txt --output-signature dist/checksums.txt.sig --output-certificate dist/checksums.txt.pem

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/portlyn-linux-amd64
            dist/portlyn-linux-arm64
            dist/portlyn-darwin-amd64
            dist/portlyn-darwin-arm64
            dist/portlyn-windows-amd64.exe
            dist/portlyn-windows-arm64.exe
            dist/portlyn-nodeagent-*
            dist/checksums.txt
            dist/checksums.txt.sig
            dist/checksums.txt.pem

  publish-containers:
    name: Publish Container Images
    runs-on: ubuntu-latest
    needs: build-release
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract backend image metadata
        id: meta_backend
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/invaliduser231/portlyn
          tags: |
            type=semver,pattern={{raw}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha
          flavor: |
            latest=true

      - name: Build and push backend image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta_backend.outputs.tags }}
          labels: ${{ steps.meta_backend.outputs.labels }}
          provenance: false

      - name: Extract frontend image metadata
        id: meta_frontend
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/invaliduser231/portlyn-frontend
          tags: |
            type=semver,pattern={{raw}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha
          flavor: |
            latest=true

      - name: Build and push frontend image
        uses: docker/build-push-action@v7
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta_frontend.outputs.tags }}
          labels: ${{ steps.meta_frontend.outputs.labels }}
          provenance: false

      - name: Extract node agent image metadata
        id: meta_nodeagent
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/invaliduser231/portlyn-nodeagent
          tags: |
            type=semver,pattern={{raw}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha
          flavor: |
            latest=true

      - name: Build and push node agent image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile.nodeagent
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta_nodeagent.outputs.tags }}
          labels: ${{ steps.meta_nodeagent.outputs.labels }}
          provenance: false
